name: Android Debug

on:
  workflow_dispatch:
  pull_request:
    branches:
      - '**'

jobs:
  build-debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/android-build
        with:
          mode: debug
